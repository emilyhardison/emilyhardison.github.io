# options specified here will apply to all posts in this folder

# prevents posts from re-rendering unless explicitly told to do so (for more, see https://quarto.org/docs/projects/code-execution.html#freeze)
freeze: true

# enable banner style title blocks (see https://quarto.org/docs/authoring/title-blocks.html)
title-block-banner: true




# remove table of contents on the page
toc: false

format:
  html:
    theme: 
      #- morph # if using custom style 
      - styles.scss # using custom theme overlay
    #css: styles.css
    toc: false
    #page-layout: default  # spreads out the content on the page
    mainfont: Lato 